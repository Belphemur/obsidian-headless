services:
  docs:
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - ./src/.vuepress/dist:/usr/share/nginx/html/obsidian-headless:ro
      - ./nginx.docs.conf:/etc/nginx/conf.d/default.conf:ro
    restart: "no"
